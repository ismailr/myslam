#include "layout_prediction/optimizer.h"
#include "layout_prediction/graph.h"

Optimizer::Optimizer (Graph* graph){}

void Optimizer::run (){
    while(1)
    {
        std::cout << "optimizer is running." << std::endl;
    }
}
